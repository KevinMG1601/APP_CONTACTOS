import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.university.missionsapp',
  appName: 'Misiones Campus',
  webDir: 'dist',
};

export default config;
